
```{r build-supfig-series-numbers}
#| fig-asp: 0.618
#| out-width: "90%"
#| fig-width: 9

build_supfig_timeseries_number(
    tar_read(
        population_variability_data,
        store = store_in_pipeline()
    ),
    tar_read(
        aggregate_survey_filtered,
        store = store_in_pipeline()
    ),
    tar_read(
        all_local_trends_outputs,
        store = store_in_pipeline()
    ) 
)
```