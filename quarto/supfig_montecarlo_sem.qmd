
```{r}
#| fig-asp: 1.618
#| out-width: "65%"
#| fig-width: 6.5

n_rep_ok <-  tar_read(
        mc_summaries_sem_10000,
        store = store_in_pipeline()
    ) %>%
    group_by(HABITAT) %>%
    summarise(n_ok = unique(n_replicates_ok)) %>%
    pivot_wider(names_from = HABITAT, values_from = n_ok)

build_supfig_montecarlo_sem(
    tar_read(
        spatial_sem_output,
        store = store_in_pipeline()
    ),
    tar_read(
        mc_summaries_sem_10000,
        store = store_in_pipeline()
    )
)
```