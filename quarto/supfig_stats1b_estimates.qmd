```{r build-supfig-stats1-estimates}
save_and_output(
  build_fig_stats1_parameters(
  tar_read(
    models_stab_across_habitats,
    store = store_in_pipeline()
  ),
  model_name = "full"
),
filename = here::here("output/Fig_S4.png"),
width = 7,
height = 0.618 * 7,
unit = "in"
)
```

